/* tslint:disable:no-unused-variable */

import {
    async,
    TestBed
} from '@angular/core/testing';

import { NgxTextEqualityValidatorDirective } from './ngx-text-equality-validator.directive';

describe('NgxTextEqualityValidatorDirective', () => {
    it('', () => {
        const directive = new NgxTextEqualityValidatorDirective();

        expect(directive).toBeTruthy();
    });
});
